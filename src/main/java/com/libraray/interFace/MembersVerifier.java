package com.libraray.interFace;

//interface shows the data which are accessible from outside the object
public interface MembersVerifier {
    boolean aadhaarVerifier();

    boolean amountManager();
}
